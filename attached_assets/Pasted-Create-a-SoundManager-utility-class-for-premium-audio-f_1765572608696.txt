Create a SoundManager utility class for premium audio feedback, similar to high-end Web3 apps.

SOUND EVENTS:
1. mint_success - Celebration sound (short, triumphant)
2. mint_legendary - Special fanfare for legendary mints
3. click - Subtle UI click
4. hover - Very soft whoosh
5. notification - Gentle chime for new activity
6. error - Soft error tone
7. connect_wallet - Success connection sound

IMPLEMENTATION:
class SoundManager {
  constructor() {
    this.sounds = new Map();
    this.enabled = localStorage.getItem('soundEnabled') !== 'false';
    this.volume = parseFloat(localStorage.getItem('soundVolume') || '0.5');
    this.preloadSounds();
  }

  preloadSounds() {
    // Use Web Audio API for low-latency playback
    // Fallback to Audio elements
  }

  play(soundName) {
    if (!this.enabled) return;
    // Play sound with current volume
  }

  setEnabled(enabled) {
    this.enabled = enabled;
    localStorage.setItem('soundEnabled', enabled);
  }

  setVolume(volume) {
    this.volume = Math.max(0, Math.min(1, volume));
    localStorage.setItem('soundVolume', this.volume);
  }
}

SOUND FILES:
- Use royalty-free sounds or generate with Web Audio API oscillators
- Keep files small (< 50KB each)
- Format: MP3 with OGG fallback
- Alternatively, generate sounds programmatically using Web Audio API:
  - Click: Short sine wave 800Hz, 50ms, quick fade
  - Hover: White noise, very low volume, 100ms
  - Success: C major chord arpeggio
  - Error: Descending two-tone

UI CONTROLS:
- Sound toggle button in header (speaker icon)
- Muted state: 🔇 icon
- Enabled state: 🔊 icon
- Click to toggle
- Long press/right-click for volume slider

HAPTIC FEEDBACK (mobile):
- Use navigator.vibrate() API where supported
- Light vibration on clicks: vibrate(10)
- Success vibration: vibrate([50, 50, 100])
- Error vibration: vibrate([100, 50, 100])

INTEGRATION POINTS:
- NFT card hover → hover sound
- Button clicks → click sound  
- Wallet connect success → connect_wallet sound
- New mint in activity feed → notification sound
- User mints NFT → mint_success or mint_legendary sound